apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  labels:
    app: todo-postgres
    tier: database
data:
  POSTGRES_DB: "todo_db"
  POSTGRES_USER: "todo_user"
  PGDATA: "/var/lib/postgresql/data/pgdata"
