apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    app: todo-postgres
    tier: database
spec:
  type: ClusterIP
  selector:
    app: todo-postgres
  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      targetPort: 5432
