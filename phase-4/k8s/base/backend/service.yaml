apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: todo-backend
    tier: api
spec:
  type: ClusterIP
  selector:
    app: todo-backend
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
