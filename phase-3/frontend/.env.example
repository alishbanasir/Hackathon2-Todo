# Phase 3 Frontend Environment Variables

# Phase 3 Backend API (Todo AI Chatbot with Gemini)
# Note: Do NOT include /api/v1 - the frontend code adds it automatically
NEXT_PUBLIC_API_URL=http://localhost:8008

# Phase 2 Backend API (REST API for auth - if separate)
# Note: Do NOT include /api/v1 - the frontend code adds it automatically
NEXT_PUBLIC_PHASE_2_API_URL=http://localhost:8008
